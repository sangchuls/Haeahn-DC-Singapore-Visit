# Trip Field Assistant 인수인계

## 개인 API 키 (2026-09-24)
- 채팅창 AI 설정에서 개인·Gemini/Claude/GPT 선택, 공식 키 발급 링크, 암호화 저장/잠금 해제/잠금/삭제/연결 확인 제공.
- 키는 브라우저 로컬에 AES-GCM으로 암호화. 비밀번호는 저장·전송하지 않음. 개인·제공사 변경 시 복호화 키 제거, 개인 변경 시 이전 대화 초기화. 키와 비밀번호를 대화 이력에 포함하지 않음.
- api/claude.js의 assistant 모드는 개인 x-ai-key 필수. 고정된 제공사별 API로만 전달, redirect 차단, no-store, 키 로깅/저장/응답 반사 없음. 기존 day scenario 계약과 공용 키 폴백은 유지.
- 키 누락/잠김은 설정 화면 안내. 인증/권한/잔액·할당량/모델/타임아웃 오류를 구분. 날짜 지정 일정 질문은 API 없이 처리.
- 자동 테스트 22개 통과: 실제 WebCrypto 저장/복호화·개인 분리·잘못된 비밀번호·삭제, provider별 요청/응답·오류, 키가 prompt/응답에 들어가지 않는지 검사 포함.
- 실제 개인 API 키 미보유로 유료 live 호출 미실시. 연결 확인 버튼으로 사용자 계정의 사용 가능 여부 확인 필요. 실제 모바일 화면·음성 검증도 별도 필요.

## 채팅 UX 수정 (2026-09-24)
- 사용자 요청에 따라 일정 화면 상단의 Assistant/NOW/NEXT 요약 박스 삭제.
- 하단 버튼 → 말풍선 채팅창. 대화 로그, 하단 입력창, 음성 대화 시작/중지, 답변 읽기 후 재청취. 모바일 키보드 높이에 대응.
- 날짜·출장자·지도·메모 도구는 접힌 메뉴에 유지. 후속 질문에 최근 8개 대화 전달, 서버 역할/길이 검증.
- 자동 테스트 18개 통과: 대화 누적·후속 문맥, 모의 음성 인식/읽기/재청취·닫기 종료·권한 거부 포함. 연결 가능한 브라우저가 없어 실제 모바일 렌더링/마이크 검증은 별도 필요.

## 기준 및 분석
- 시작 브랜치 main, 로컬/원격 HEAD: 820fe0b56fe335dfe537dbaf483c2388ce3f6a5d. 작업 전 변경 없음.
- index.html: 내장 Leaflet, DAYS 일정, TR 교통편, 월요일 선택 경로, 준비 체크리스트, 학습노트, 맛집, 지도 애니메이션·이전동선, AI 시나리오 및 내장 폴백.
- api/claude.js: Vercel 서버 함수, ANTHROPIC_API_KEY 사용. 클라이언트 키 없음.
- sw.js / manifest.json: 오프라인 PWA. vercel.json: 빌드 없는 정적 배포, sin1. 실제 Vercel 환경변수 설정 여부는 계정에서 확인 필요.
- 항공권 PDF 4명분을 2026-09-24에 대조하여 기존 귀국편·터미널·수하물 오류를 교정. 검증 범위는 FLIGHT_VERIFICATION.md 참고.

## 최소 변경 계획
1. 기존 DAYS/TR 구조와 화면 유지. 별도 어댑터로 WHEN/WHERE/HOW/ARRIVAL/WHAT/ACTION/PEOPLE/MATERIAL/NEXT/NOTE 구조 및 필드별 출처·검증 상태 제공.
2. KDCEA 9/29 목적지와 왕복 교통편은 unverified/reference_only. 관련 외부 길찾기 중단, 기존 지도에는 참고 위치 경고.
3. 하단 Assistant 및 NOW/NEXT 요약 추가. 기존 데이터로 오프라인 질문 처리, 선택적 기존 Claude API 기반 자연어 답변. 미확정 값은 답변에서 명시.
4. 브라우저 지원 시 음성 인식/읽기. 지원·권한·네트워크 실패 시 텍스트 유지.
5. DC 체크리스트 및 일정별 로컬 현장 메모. 기존 날짜/경로/시나리오 기능 연결.
6. 날짜 경계, KDCEA 가드, 항공 필드별 검증 상태, 월요일 경로 및 API 실패 테스트. feature 브랜치 commit/push 후 실제 모바일 검증은 별도로 진행.

## 데이터 원칙
기존 앱 계획은 provisional로 유지한다. 항공권에서 확인한 항공편·시각·좌석·수하물·기재된 터미널만 e-ticket/verified로 관리한다. 출국편 싱가포르 도착 터미널은 공란이므로 unverified이며, 좌표·집결 계획·이동시간도 항공권으로 검증된 것으로 승격하지 않는다. KDCEA 목적지는 확정 이메일 확인 전까지 unverified다. 날짜는 싱가포르/조호르 UTC+8, 한국 출도착은 UTC+9로 계산한다. 종료시각 미등록 일정은 실제 진행 중이라고 단정하지 않는다.

## 구현 결과 / 변경 파일
- index.html: 기존 단일 파일 오프라인 사용 유지. FieldAssistant 어댑터, NOW/NEXT, 일정 선택, 오프라인 질문, 자연어 API 폴백, 브라우저 음성 입력/읽기, 현장 메모·체크리스트, 기존 일정/경로/시나리오 연결.
- index.html: KDCEA·미정 행사장 길찾기 차단. 출국 도착 터미널과 기존 공항 PIN은 미확정 표시. 귀국 T2는 이름으로 길찾기하며 T3 좌표를 재사용하지 않음. 일정·체크리스트·시나리오·AI 문맥의 항공정보를 일치시킴.
- api/claude.js: 기존 시나리오 응답 유지, assistant 모드 추가, 신뢰도 시스템 지시, 길이 제한·10초 타임아웃·오류 상세 비노출.
- sw.js: 캐시 버전 갱신. vercel.json / manifest.json / API 키 이름 유지.
- package.json 및 tests/: Node 내장 테스트 러너, 신규 런타임 의존성 없음.

## 검증 결과 (2026-09-24)
- npm test: 15개 통과. 인라인 JavaScript 문법, 6일 일정 유지, KDCEA 안내·길찾기 가드, UTC+8 날짜 경계와 한국 출도착, 4명 좌석/수하물·항공권 출처·도착 터미널 공란, T2 이름 길찾기/좌표 분리, 구 항공정보 잔존 검사, 월요일 코스, schema, 시나리오 안전 문맥, 서버 계약/오류, Assistant 초기화/오프라인/메모 저장/API 실패 복구.
- git diff --check 통과.
- API 테스트는 모의 응답으로 수행. 실제 Vercel API 키, 실시간 모델 호출, 배포 결과는 미검증.
- Browser 런타임 연결 후 브라우저 목록이 비어 있어 실제 모바일 화면·음성 인식·지도 애니메이션 회귀 테스트는 미실시. DOM 계약 테스트로 초기화와 주요 이벤트 흐름만 검증.
- e-Ticket 4명분 1페이지를 텍스트 및 렌더 이미지로 대조 완료. 영수증은 항공 운항정보의 근거로 사용하지 않음. PDF·결제정보·예약번호·티켓번호는 Git에 포함하지 않음. 참관단 확정 이메일은 미첨부이므로 KDCEA 목적지 확정 보류.
- feature 브랜치에 작업 보존. 운영 main 병합 및 운영 배포는 이 변경에 포함하지 않음.

## 실제 기기에서 남은 확인
1. 390px 내외 휴대폰에서 모달, 키보드, 닫기, 날짜 선택, 기존 날짜/학습/맛집 탭과 지도·현재/이전 경로 확인.
2. 마이크 허용/거부, 미지원 브라우저, 답변 읽기 중지, 비행기 모드, 저장 후 재접속 확인.
3. Vercel Preview에서 기존 시나리오 및 자유형 AI 질문 성공/오류 확인. ANTHROPIC_API_KEY는 서버 환경변수에만 설정.
4. 운항 당일 출국 도착 터미널·탑승구·카운터 변경 확인. KDCEA 확정 이메일 확보 후 실제 목적지·집결지 반영.
