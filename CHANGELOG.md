# 변경 이력

## v2.0 — 전체 점검 및 최적화 (2026-09-19)

### 치명적 결함 수정

**1. 지도 라이브러리 미로드 시 앱 전체 크래시**
Leaflet을 CDN에서 불러오던 구조라, 오프라인·기내·CDN 차단 환경에서
`L is not defined`로 앱이 렌더링조차 되지 않았습니다.
탭 0개, 일정 0개 — 사실상 사용 불가 상태였습니다.

- Leaflet 1.9.4(CSS+JS)를 파일에 인라인 임베드 → 외부 의존성 제거
- `initMap`에 미로드 가드 추가, 실패 시 지도 영역에 안내 문구 표시
- `pinIcon` · `drawDayRoute` · `switchDay`에 `mapReady` 가드 추가

검증 결과 — Leaflet 제거 상태에서:
| | 수정 전 | 수정 후 |
|---|---|---|
| 탭 렌더링 | 0개 | 6개 |
| 일정 렌더링 | 0개 | 정상 |
| JS 에러 | 1건 (치명적) | 0건 |

**2. Next.js 배포본이 초기화되지 않음**
`app.js`를 `strategy="afterInteractive"`로 로드했는데, 이 시점엔
`DOMContentLoaded`가 이미 지나 초기화 리스너가 영원히 실행되지 않았습니다.
추가로 `dangerouslySetInnerHTML`에 담긴 61KB의 스크립트가 실행되지 않는 채로
페이지 용량만 차지했습니다.

→ Next.js를 걷어내고 **정적 파일 + 서버리스 함수** 구조로 재편.
빌드가 사라지고, 오프라인 파일과 배포본이 완전히 같은 코드가 됐습니다.

### 기능 버그 수정

**3. 투어 일정의 점 색상 오류**
`'d' + type[0]`로 CSS 클래스를 만들어 `travel`과 `tour`가 모두 `.dt`로
충돌했습니다. Brightray DC 투어·Schneider 투어가 주황이 아닌 파랑으로
표시되고, `.dtr` 규칙은 쓰이지 않았습니다.
→ `DOT_CLASS` 매핑 테이블로 교체.

**4. 창이공항 좌표 불일치**
일정 데이터는 `1.3592, 103.9896`, 교통편 데이터는 `1.3644, 103.9915`로
약 600m 어긋나 경로 시작점이 실제 터미널과 달랐습니다.
→ T3 기준으로 통일 (5곳).

### 메모리 · 성능

**5. 이전 동선 무한 누적**
날짜를 바꿀 때마다 회색 경로선이 지도에 쌓이기만 하고 제거되지 않았습니다.
→ `pushPastLine` 헬퍼 도입, 최대 12개 유지. 40회 전환 테스트에서
레이어 증가 0건 확인.

**6. 중복·미사용 코드 제거**
- `dotColors` 객체가 세 곳에 중복 정의 → `DOT_COLOR` 상수로 통합
- 미사용: `TR_COLOR`, `TR_LABEL`, `pastMarkers`, `simAbortCtrl`

### 신규 기능

- **하이브리드 AI 시나리오** — 웹에서는 Claude 실시간 생성, 파일/오프라인에서는
  내장 시나리오. 12초 타임아웃 후 자동 폴백. 화면에 출처를 표시합니다.
- **PWA** — `manifest.json` + Service Worker. 홈 화면에 추가해 앱처럼 쓰고,
  한 번 방문하면 오프라인에서도 열립니다.
- **지도 실패 안내 UI** — 지도를 못 불러와도 나머지 기능을 쓸 수 있음을 알립니다.
- iOS 사파리 `100dvh` 대응 — 주소창 때문에 하단이 잘리던 문제 해결.

### 검증

Playwright(모바일 뷰포트)로 전 기능 자동 테스트:
6일 전체 순회 · 교통편 3종 경로 그리기 · 당일 동선 · AI 시나리오 30개 ·
월요일 루트 3종 · 딥링크 URL · 오프라인 전환 · 메모리 누수 40회 반복.
**JS 에러 0건.**


## v2.1 — 실사용 시나리오 테스트에서 발견된 버그 수정 (2026-09-19)

### 치명적: AI 시나리오 패널을 닫을 수 없음

실제 터치로 6일 여정을 재현하던 중, **시나리오 패널의 X 버튼이 눌리지 않아
앱이 먹통이 되는 문제**가 드러났습니다. 함수 호출로는 정상이라 이전 검증에서
잡히지 않았고, 실제 손가락 터치로만 재현되는 버그였습니다.

원인은 z-index 겹침이었습니다. Leaflet 줌 컨트롤(`.leaflet-top`)이 `z-index:1000`을
가지는데 `#mapWrap`이 stacking context를 만들지 않아 이 값이 그대로 최상위에서
비교됐고, 시나리오 패널(950)보다 위에 렌더링됐습니다. 하필 패널의 X 버튼과
줌 버튼이 화면 우상단 같은 자리라 정확히 겹쳤습니다.

패널이 닫히지 않으니 그 뒤 날짜 전환·일정 확인이 모두 막혔습니다.
9/30, 10/1, 10/2 일정에 아예 접근할 수 없는 상태였습니다.

수정:
- `#mapWrap`에 `isolation:isolate` + `z-index:1` — 지도 내부 z-index를 가둠
- 패널 z-index 상향 (미리보기 2000, 시나리오 2100)
- 패널이 열린 동안 지도 컨트롤 비활성화 (`body.panel-open`)

### 이전 동선 누수 잔여분

`switchDay`의 경로 보관이 상한 헬퍼를 거치지 않아 `pastLines`가 계속 쌓였습니다.
5라운드에서 20개까지 증가(상한 12 초과). 헬퍼 경유로 통일해 12개에서 고정됩니다.

### 실사용 시나리오 테스트 결과

실제 출장자 관점의 6일 전체 여정을 모바일 뷰포트에서 **실제 터치 조작으로**
5회 반복했습니다. 온라인(배포본)과 오프라인(파일) 양쪽 모두.

| | 온라인 | 오프라인 |
|---|---|---|
| 검증 항목 | 235건 | 235건 |
| 통과 | 235건 | 235건 |
| JS 에러 | 0건 | 0건 |
| 라운드 소요 | 16.4~16.6초 (안정) | 16.7~16.9초 (안정) |
| pastLines | 12개 고정 | 12개 고정 |
| 힙 증가 | 0.0MB | 0.0MB |

검증한 실사용 동작: 체크인 마감·편명·수하물 확인, 창이공항 입국 절차와 T3 출구,
Grab 영문 주소와 버튼 3종, 공항→호텔 경로, 구글맵 네비 URL, 월요일 루트 3종 전환,
여권 경고, 투어 점 색상, 국경 절차, DC 관찰 포인트, 당일 동선 미리보기,
AI 시나리오 생성·토글·닫기, Schneider 주소, 카운터 마감 시각, 귀국 일정.


## v2.2 — 출발 전 준비 체크리스트 (2026-09-19)

앱 첫 탭에 **준비 체크리스트**를 넣었습니다. 39개 항목을 6개 묶음으로
시간 순서대로 정리했고, 체크 상태는 기기에 저장되어 며칠에 나눠 준비해도
이어집니다.

| 묶음 | 항목 | 내용 |
|---|---|---|
| D-7 이전 · 서류와 예약 | 6 | 여권 유효기간, DCW 등록 QR, e-티켓, 호텔 확인서, 보험 |
| D-7~D-3 · 통신과 앱 | 5 | 로밍·eSIM, Grab 등록, 오프라인 지도, 앱 파일 저장 |
| D-3~D-1 · 업무 준비 | 6 | 영문 명함, 세션 일정, DC 투어 질문, 영어 회사 소개 |
| D-1 · 환전과 짐 | 10 | SGD 환전, 타입G 어댑터, 보조배터리, 우기 대비, 복장 |
| D-1 · 마지막 확인 | 7 | 온라인 체크인, 여권 가방에, 수하물 무게, 담배 반입 금지 |
| 9/27 당일 아침 | 5 | 집결 시각, 카운터 마감, 기내 입국카드 |

놓치면 실제로 문제가 되는 9개 항목에 **필수** 표시를 붙였습니다 —
여권 유효기간, Grab 사전 등록, 앱 파일 저장, 영문 명함, SGD 현금,
보조배터리 기내 휴대, 여권 지참(조호바루), 담배 전량 제외, 카운터 마감 시각.

### 구현

- 진행률 막대와 묶음별 카운터. 체크할 때 전체를 다시 그리지 않고 숫자만 갱신
- `localStorage` 저장, 접근이 막힌 환경에서도 세션 내에서는 정상 동작
- 체크리스트 탭에서는 지도를 감추고, 날짜 탭으로 돌아가면 `invalidateSize()`로 복구
- 날짜 인덱스가 1씩 밀리면서 `SCENARIOS` 키도 함께 재배열

### 검증

체크리스트 26건 전항목 통과. 기존 실사용 5회 반복 테스트(235건)도
회귀 없이 전부 통과, JS 에러 0건.


## v2.3 — 자전거 라이딩 루트와 맛집 탭 (2026-09-19)

### 월요일 자유일 — 루트 D: 공유자전거 라이딩

싱가포르 최대 공유자전거 **Anywheel**로 마리나베이 워터프론트를 도는
약 11km 코스를 추가했습니다. 9개 지점, 3~4시간.

라우파삿(1894년 주철 구조) → 머라이언 파크 → 주빌리 브리지 → 에스플러네이드
→ 헬릭스 브리지 → 싱가포르 플라이어 → 가든스 바이 더 베이 외곽
→ **마리나 배러지**(댐+공원 복합 인프라, 이 코스의 하이라이트) → 복귀.

실제로 타려면 알아야 하는 것들을 전부 넣었습니다:

- 요금 S$1/30분, **7일 패스 S$6.90**(출장 기간에 딱 맞음), 최소 충전 S$10, 보증금 없음
- **반납은 앱의 P 아이콘(지정 주차소)에서만** — 아무 데나 세우면 요금이 계속 붙고 S$5 벌금
- 주차 후 **주차소 QR을 한 번 더 스캔**해야 반납 완료
- 노란 박스라고 전부 주차소는 아님

안전 규정도 반영했습니다. 싱가포르는 **차도 주행 시 헬멧이 의무**인데
공유자전거는 헬멧을 주지 않으므로, 코스를 전 구간 프롬나드·공유로로만 짰습니다.
헬릭스 브리지와 주빌리 브리지는 자전거를 끌고 건너야 한다는 점도 명시했습니다.

### 맛집 · 자투리 시간 탭

"지금 뭐 먹지", "1시간 뜨는데 뭐하지"를 그 자리에서 해결하는 탭입니다.
14곳을 네 묶음으로 정리했습니다.

| 묶음 | 내용 |
|---|---|
| 호텔 근처 | 맥스웰·아모이·라우파삿·탄종파가 한식당거리·차이나타운 콤플렉스 |
| 행사장 근처 | MBS 푸드코트, 가든스 바이 더 베이 호커센터 |
| 1~2시간 남았을 때 | URA 도시갤러리, 덕스턴힐 숍하우스, 피너클 스카이브리지, 마리나 배러지 |
| 알아두면 좋은 것 | 호커센터 이용법, 가격 감각, 더위 대처 |

각 카드에 정확한 주소(우편번호 포함), 영업시간, 도보 시간, 가격대, 현장 팁이 들어갑니다.
좌표는 구글 플레이스로 검증했고, 카드를 누르면 지도에 표시되며 도보 길찾기로 바로 연결됩니다.

실제로 겪는 함정을 팁에 담았습니다 — 맥스웰은 카드 결제 시 건당 50센트가 더 붙고,
아모이는 인기 노점이 14:30이면 닫으며, 차이나타운 콤플렉스는 2층이 식당가입니다.
URA 도시갤러리는 일요일 휴관이라 9/27에는 갈 수 없습니다.

### 검증

신규 기능 30건 전항목 통과. 기존 실사용 5회 반복 테스트도 240건 전부 통과,
JS 에러 0건.


## v2.4 — 월요일 루트 구글맵 경로 연동 (2026-09-19)

월요일 4개 루트를 **지도를 보면서 실제로 이동할 수 있게** 만들었습니다.

### 구간별 길찾기

스팟 카드를 펼치면 **이전 지점 → 이 지점** 구간의 길찾기 버튼이 나옵니다.
누르면 구글맵이 해당 구간 경로를 띄운 채로 열립니다.

| 루트 | 버튼 |
|---|---|
| 도시건축 · 문화역사 · 인프라DC | 🚗 Grab · 🚇 MRT·버스 · 🚶 도보 |
| 자전거 라이딩 | 🚲 자전거 · 🚗 Grab · 🚶 도보 |

첫 지점은 출발점이라 구간 길찾기가 없고, 대신 장소 자체를 여는 버튼만 있습니다.

### 전체 경로 한 번에

루트 배너에 **🔵 구글맵 전체 경로** 버튼을 넣었습니다. 그날 도는 모든 지점이
경유지로 들어간 상태로 구글맵이 열립니다. 루트별 기본 이동수단이 자동 적용되고,
자전거 루트는 호텔에서 출발해 호텔로 돌아오는 순환 경로로 잡힙니다.

구글맵 api=1은 경유지를 9개까지만 받아서, 그보다 많으면 균등 간격으로
추려 넣도록 했습니다.

### 검증

신규 57건 전항목 통과 — 4개 루트 × (전체 경로 URL · 경유지 개수 · 이동수단 3종 ·
모드 정확성 · origin/destination · 첫 지점 예외 처리).
기존 실사용 5회 반복 240건도 회귀 없이 통과, JS 에러 0건.


## v2.5 — 숙소 좌표 정정 · 내부 정보 제거 · 출발 동선 보강 (2026-09-19)

### 숙소 좌표가 260m 틀렸습니다

앱이 쓰던 좌표 `1.2769, 103.8421`이 실제 위치와 약 260m 어긋나 있었습니다.
구글 플레이스로 대조해 `1.275835, 103.8442824`로 바로잡았습니다.
우편번호도 `079312`가 아니라 **`079333`**이 맞습니다.

Grab 기사에게 불러줄 주소와 모든 길찾기 출발·도착점이 이 좌표를 쓰기 때문에,
공항에서 호텔로 갈 때와 마지막 날 공항 갈 때 엉뚱한 곳으로 안내될 수 있었습니다.
21곳을 전수 교체했습니다.

정확한 주소: **Oasia Hotel Downtown, 100 Peck Seah St, Singapore 079333**
가까운 역은 Tanjong Pagar(EW15, 도보 3분)와 Maxwell(TE18, 도보 7분)입니다.

### 내부 프로젝트 정보 제거

담당 프로젝트명과 용량이 체크리스트·투어 팁·AI 시나리오 대화에 흩어져 있었습니다.
앱은 링크 하나로 공유되고 현지에서 화면을 보여줄 일도 있어 전부 일반 표현으로 바꿨습니다.

- 프로젝트명 → "담당 프로젝트"
- 구체적 용량(40MW / 60MW / 145MW, 지역명) → "하이퍼스케일 프로젝트"
- 영어 피치 문장에서도 용량 삭제

방문 대상인 Brightray 시설의 용량은 현지 공개 정보이므로 그대로 뒀습니다.

### 월요일 루트에 숙소 출발·복귀 추가

네 루트 모두 **첫 목적지부터 시작**해서, 숙소에서 거기까지 어떻게 가는지가
비어 있었습니다. 각 루트 앞뒤에 숙소 지점을 넣었습니다.

| 루트 | 숙소 → 첫 목적지 |
|---|---|
| 도시건축 | MRT 약 20분 S$1.5 / Grab 약 10분 S$9~13 |
| 문화·역사 | 도보 12분 / MRT 1정거장 / Grab 5분 S$6~9 |
| 인프라·DC | MRT+버스 약 50분 / **Grab 25분 S$18~25 권장** |
| 자전거 | 호텔 앞 Anywheel 거치대에서 대여 |

복귀 동선도 넣었습니다. 문화·역사 루트는 마지막 지점(URA 도시갤러리)이
숙소에서 도보 5분이라 복귀가 가장 편하고, 인프라·DC 루트는 이동 거리가 길어
출발을 08:45로 앞당겼습니다.

이제 각 루트의 **구글맵 전체 경로**가 숙소에서 출발해 숙소로 돌아오는
왕복 경로로 잡히고, 첫 목적지 카드에 숙소 출발 구간 길찾기가 붙습니다.

### 검증

신규 24건 전항목 통과 — 내부정보 노출 0건, 좌표 전수 교체, 4개 루트 출발/복귀,
길찾기 출발지 정확성, 날짜별 첫 일정 숙소 출발 여부.
기존 실사용 5회 반복 240건도 회귀 없이 통과, JS 에러 0건.
